// import codError from '../CodeError'

// const emptyError = (err: codError, _req: Request, res: Response) => {
//   if (!password || !mail) {
//     return res.status(400).json({ message: 'All fields must be filled' });
//   }
// };

// export default middwareError;
